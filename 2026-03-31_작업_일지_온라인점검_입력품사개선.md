# 2026-03-31 작업 일지 — 온라인 점검·입력·품사 개선

## 📅 개요
- **작업일**: 2026년 3월 31일
- **상태**: ✅ 기록 완료 (사용자 실행 확인 포함)

---

## 1. GitHub Pages 온라인 점검 (32 유닛)
- **방법**: `https://word1600.github.io/game/unit1/` ~ `unit32/` 에 대해 HTTP HEAD 요청으로 응답 코드 확인.
- **결과**: **32개 유닛 진입 URL 모두 200** (페이지 배포 정상).
- **데이터 경로 참고** (HEAD 기준):
  - `/game/data/unit2.json` → **404** 가능 (저장소에 `Unit2.json` 등 **대소문자** 차이 시 Linux/Pages에서 불일치).
  - `/game/unit28/data/unit28.json` → **404** 가능 (유닛 폴더 내 `data` 미러 없을 때).
  - 각 유닛 `game.js`가 실제로 쓰는 URL(예: `unit2/data/unit2.json`, `../data/unit28.json`)이 **200**이면 게임은 정상 로드됨.
- **사용자 확인**: **Unit 2, 28, 32** 직접 실행 — **이상 없이 정상 동작** 확인.

---

## 2. 품사 표시 개선 (전 유닛)
- **목적**: JSON에 `n.`, `v.`, `adj.` 등 짧은 표기만 있어도 화면에 `n.(명사)` 형태로 표시.
- **구현**: 각 `unit*/game.js` 및 루트 `game.js`에 `normalizePosForDisplay()` 추가, `#word-pos`에 적용.
- **Git**: 커밋 예 — `feat: normalize short POS labels...` (`162bc62` 부근).

---

## 3. 보너스 타이핑 챌린지 — 영문 입력 유도
- **목적**: 보너스 구간에서 한글 IME로 잘못 입력되는 경우 완화.
- **구현** (보너스 `#challenge-input` 한정):
  - `lang="en-US"`, `inputmode="latin"`, `translate="no"`, `dir="ltr"`, `autocorrect="off"` 등.
  - `input` / `compositionend`에서 **한글 음절·자모 제거** (브라우저가 IME를 강제 전환하진 못함 — 사용자 한/영 전환은 기존과 동일).
- **Git**: 커밋 예 — `feat: bonus challenge input defaults to English...` (`36b1aca` 부근).
- **도구**: `tools/patch_challenge_english_input.py`

---

## 4. 다음 작업: Unit 33
1. **`data/unit33.json`** 준비 (파일명 **소문자** `unit33.json`).
2. 채팅에 **`Unit 33 진행`** 요청.
3. 절차는 **Unit 32**와 동일: `unit32` → `unit33` 복사, `UNIT33_DATA_URLS`, `unit33/data/unit33.json`, `index.html` 버전 bump, MD·커밋·푸시.

---

## 📎 관련 파일·커밋
- 품사: `tools/patch_pos_display.py` (이전 작업)
- 챌린지 입력: `tools/patch_challenge_english_input.py`
- 상세 유닛 작업: `2026-02-22_Unit32_생성_및_검토_완료.md`, `2026-02-22_작업_일지_Unit32_완료.md` 등

---

**작성**: 2026-03-31  
**비고**: 전 유닛을 매번 수동 실행하지 않아도, 배포 후 **HEAD 점검 + 샘플 유닛(예: 2·28·32) 확인**으로 운영 가능.
