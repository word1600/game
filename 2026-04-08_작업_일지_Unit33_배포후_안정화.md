# 2026-04-08 작업 일지 — Unit 33 배포 후 안정화

## 개요
- Unit 33 생성/배포 이후 타이핑 입력 관련 이슈 대응 및 안정화 기록
- 최종 상태: ✅ 정상 동작 확인

## 핵심 이슈
- 현상: 보너스 타이핑 입력이 되지 않는 것처럼 보임
- 원인: 한글 입력을 즉시 제거하는 필터(`input`/`compositionend`)가 강하게 적용되어, 한/영이 한글 상태일 때 입력이 바로 사라짐

## 조치 내용
1. `unit1`~`unit33`의 `game.js`에서 한글 강제 제거 로직 제거
2. 타이핑 판정 로직을 이전 방식(기존 비교)으로 복원
3. 캐시 반영을 위해 각 유닛 `index.html`의 `game.js?v=` 버전 증가
4. 배포 반영 완료

## 배포 커밋
- `84c0ec1`  
  - 메시지: `fix: revert challenge typing behavior to previous input handling`

## 검증
- 사용자 스팟 체크: **5개 유닛**
- 결과: **모두 정상 실행**
- 입력 동작: 초기 한글 상태일 경우 한/영 전환 후 정상 입력 가능 (기존 동작과 동일)

## 결론
- 현재 운영 기준으로 안정화 완료
- 다음 Unit 34부터는 동일 절차로 생성 가능

